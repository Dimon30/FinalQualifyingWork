# Известные проблемы и варианты их решения

Файл фиксирует основную проблему, обнаруженную при бенчмарке на dataset_large (~41k строк, 2026-05-17), и возможные пути её устранения.

---

## Проблема: модели расходятся в замкнутом контуре, ‖v‖ улетает до 200+ м/с

### Симптомы

При запуске бенчмарка / `run_compare_models.py` для некоторых моделей наблюдается:

| Модель | Сценарий | e2 RMS, м | ‖v‖ финальное, м/с |
|--------|---------|-----------|---------------------|
| MLP    | spiral_r3, vstar-rate=0.5 | 900 | 217 |
| SAC    | spiral_r3, vstar-rate=0.5 | 369 | 172 |
| TD3    | spiral_r3, vstar-rate=0.3 | 207 | 232 |
| TD3    | helix_r2                  | 53  | DIVERGED |
| MLP    | helix_r2                  | 198 | DIVERGED |

Это при том, что `QuadModel.max_speed = 10` м/с. Дрон превышает этот предел в 20+ раз.

### Почему `max_speed=10` не ограничивает реальную скорость

Параметр `max_speed=10` в `QuadModel` ограничивает только **выход нейросети V*** (параметрическую скорость), но не реальное `‖v‖` дрона. Применяется в трёх местах:
1. `SpeedPredictorAny.predict()` — clip(V*, min_speed, max_speed)
2. `feature_vector` — нормировка `v_norm / max_speed`
3. Oracle в `simulator_wrapper.py` — диапазон поиска V_opt

Реальная физическая скорость определяется уравнениями динамики `quad_dynamics_16` (`code_app/drone_sim/models/dynamics.py`):
```
v̇ = (b(φ,θ,ψ) · (u1 + g) - [0, 0, g]) / mass
u1 = sat_tanh(u1_bar, L=5)   # тяга в [-5, +5]
```
**В модели нет аэродинамического сопротивления.** Дрон ускоряется свободно, нет естественного «тормоза».

### V* vs дуговая скорость

V* — это параметрическая скорость (изменение ζ во времени). Реальная дуговая:
```
‖v‖_arc = V* · ‖t(ζ)‖
```

| Кривая | ‖t‖ | При V*=10 |
|--------|-----|-----------|
| spiral r=3 | √10 ≈ 3.16 | ‖v‖ ≈ 31.6 м/с |
| line x=y=z=s | √3 ≈ 1.73 | ‖v‖ ≈ 17.3 м/с |
| helix r=2 | √5 ≈ 2.24 | ‖v‖ ≈ 22.4 м/с |
| circle r=3, z=const | 1.0 | ‖v‖ ≈ 10 м/с |

В `path_sim.py` есть safety (cap на V*):
```python
max_safe_vstar = model.max_speed / ‖t‖_curr * 1.1
```
Для spiral r=3 это даёт V* ≤ 3.48. Но это cap **на V***, не на физическое ‖v‖.

### Параметр `max_velocity_norm=6` тоже не помогает

Используется **только в oracle** (`simulator_wrapper.py: is_stable`) для пометки rollout как unstable. В реальной симуляции (`path_sim.py`, `quad_dynamics_16`) этот порог **не применяется** — нет drag, нет принудительного clip.

### Динамика расхождения (типичный сценарий)

1. NN выдаёт V*=4–5 на in-distribution точках — корректно
2. Регулятор пытается достичь ‖v‖ ≈ V*·‖t‖ ≈ 12–15 м/с
3. Из-за переходного процесса e2 кратковременно растёт
4. η-расширение (`η̇ = Ū`) накапливает большой U
5. Тяга насыщена `u1 ≈ +5` → ускорение `a ≈ 5b − g ≈ 5 м/с²` минимум
6. За 30 с активного управления `v ≈ 5·30 = 150 м/с`
7. e2 > 0.7·lateral_limit → emergency: V*=min_speed, η_reset
8. **Но** дрон уже разогнан, нет drag → инерция → продолжает движение
9. Регулятор пытается затормозить, но тяга насыщена в обратную сторону `u1 ≈ −5` → `decel ≈ 5 м/с²` → за оставшееся время затормозить с 150 м/с не успевает

### Почему MLP/SAC работают при `vstar-rate=0.3`, но расходятся при 0.5

`vstar-rate` — это максимальный темп изменения V* в секунду:
- При 0.3 V*/с: V* доходит от 1 до 4 за ~10 с, регулятор успевает стабилизировать
- При 0.5 V*/с: V* доходит до 4 за ~6 с, e2 спайкает выше 0.5 м, η-windup, diverge

В `test_suite` (бенчмарк) используется `vstar_rate=0.3`, в `run_compare_models.py` сейчас тоже дефолт 0.3 (раньше был 0.5).

---

## Варианты решения

### Вариант 1 — Добавить аэродинамическое сопротивление в модель (рекомендованный)

**Файл:** `code_app/drone_sim/models/dynamics.py:quad_dynamics_16`

**Что добавить:**
```python
v = x[3:6]
v_norm = np.linalg.norm(v)
drag_coef = getattr(model, 'drag', 0.1)   # 0.1 1/(м·с) — типичное значение для квадрокоптера
a -= drag_coef * v * v_norm                # квадратичное сопротивление
```

**Что добавить в `QuadModel`:**
```python
drag: float = 0.1   # коэффициент квадратичного аэросопротивления
```

**Плюсы:**
- Физически корректно: реальные дроны имеют drag
- Естественный «терминальный» предел: при `a_thrust = a_drag` дрон не разгоняется дальше
- Терминальная скорость для L=5, g=9.81: ‖v‖_max ≈ √((u1+g) / drag) ≈ √(15/0.1) ≈ 12.2 м/с — близко к max_velocity_norm
- Не меняет логику регулятора

**Минусы:**
- Меняется математическая модель → нужно перезапустить тесты и убедиться что они проходят
- Возможно потребуется заново обучать модели (распределение V_opt в датасете изменится)
- Отход от исходной модели диссертации

### Вариант 2 — Hard-clip скорости после RK4

**Файл:** `code_app/drone_sim/simulation/runner.py` (после rk4_step)

**Что добавить:**
```python
if model is not None:
    v = x[3:6]
    v_norm = np.linalg.norm(v)
    if v_norm > model.max_velocity_norm:
        x[3:6] = v / v_norm * model.max_velocity_norm
```

**Плюсы:**
- Жёсткая гарантия `‖v‖ ≤ max_velocity_norm`
- Тривиальная реализация
- Не меняет модель динамики

**Минусы:**
- Физически некорректно: разрыв в скорости при клиппинге → разрыв ускорения → нефизическое поведение
- Энергия не сохраняется
- Может маскировать реальные проблемы регулятора (вместо diverge — просто залипает на максимуме)

### Вариант 3 — Уменьшить уровень насыщения тяги L

**Файл:** `code_app/drone_sim/simulation/path_sim.py:SimConfig.L`

**Что изменить:**
```python
L: float = 2.0   # было 5.0
```

При L=2: `u1 ∈ [-2, +2]`, максимум ускорения `a ≈ 2 + g − g = 2` м/с² по вертикали; горизонтальное — зависит от ориентации, но в любом случае меньше.

**Плюсы:**
- Простое изменение одной константы
- Дрон физически менее «мощный», меньше разгоняется
- Регулятор остаётся тот же

**Минусы:**
- Хуже отслеживание быстрых манёвров (для тугих спиралей может не хватать тяги)
- Отход от значения в диссертации (L=5)
- Не решает проблему фундаментально: при достаточно долгой симуляции дрон всё равно разгонится
- Нужно перенастроить vstar-rate (теперь медленнее)

### Вариант 4 — Защита от η-windup в регуляторе

**Файл:** `code_app/drone_sim/simulation/path_sim.py:PathFollowingController.step`

**Что добавить:** ограничить рост η-интегратора при насыщении тяги (anti-windup):
```python
# Если Ubar насыщен — не наращиваем η в этом направлении
ubar_saturated = np.abs(Ubar) >= self.p.L * 0.95
self._eta += dt * Ubar * (1 - ubar_saturated.astype(float))
```

Или альтернатива — clamp `_eta`:
```python
self._eta = np.clip(self._eta + dt * Ubar, -ETA_MAX, +ETA_MAX)
```

**Плюсы:**
- Решает корневую причину (η-windup)
- Не меняет физику модели
- Подход из теории классических регуляторов (PID с anti-windup)

**Минусы:**
- Может ухудшить точность отслеживания в нормальных режимах
- Нужно подобрать пороги
- Не помогает если дрон уже разогнан по инерции

### Вариант 5 — Conservative inference для NN-моделей

**Файл:** `code_app/drone_sim/simulation/path_sim.py:PathFollowingController.step`

**Что добавить:** ограничить `V_nn` по статистикам датасета:
```python
# V_opt в датасете: mean=3.34, std=0.91 → cap = mean + 2*std = 5.16
V_DATASET_MAX = 5.5
V_nn = min(V_nn, V_DATASET_MAX)
```

**Плюсы:**
- Не меняет физику и регулятор
- Защищает от OOD-экстраполяции (SAC выдаёт 5.85 при больших ошибках — будет обрезано)
- Простая реализация

**Минусы:**
- Hack: захардкоженный порог из статистики датасета
- Ограничивает потолок производительности (модели не могут использовать V* > 5.5)
- Не лечит фундаментальную проблему (η-windup всё равно возможен)

### Вариант 6 — Не использовать TD3 (или переобучить с doctored loss)

TD3 расходится систематически. Проблема — детерминированный актор без entropy-регуляризации, чувствительный к OOD-точкам.

**Варианты:**
- (a) Убрать TD3 из отчёта, оставить только MLP+SAC+PPO
- (b) Обучить TD3 с гораздо меньшим lr (1e-5) и patience=5 — может не успеть переобучиться на коллапс
- (c) Добавить behavior cloning loss как regularizer: `L = TD3_loss + λ · ‖μ(s) − a_oracle‖²` (`λ=0.5`)
- (d) Использовать ensemble (среднее MLP+SAC+TD3+PPO) вместо одиночных моделей

---

## Рекомендованное решение

**Комбинация Варианта 1 (drag) + Варианта 4 (anti-windup):**

1. Добавить drag → дрон физически ограничен в скорости
2. Добавить anti-windup → регулятор стабильнее в переходных процессах
3. Перезапустить генерацию датасета и обучение моделей — теперь V_opt будет естественно ограничено физикой

Это даст:
- Физически корректную модель
- Стабильные регуляторы
- Естественные ограничения скорости без hack-ов

Объём работы: ~2-3 часа кода + ~24 ч на пересборку датасета и переобучение моделей.

---

## Что есть в текущем коде

- `max_speed=10` — ограничение **только на V*** (параметрическую), не на ‖v‖
- `max_velocity_norm=6` — порог для oracle стабильности, **не применяется** в реальной симуляции
- 4-уровневая safety в `path_sim.py` (Cap на V*, e2-backoff, sarc-backoff, emergency) — **снижают V***, но не тормозят разогнавшийся дрон
- `L=5` (sat_tanh) — насыщение тяги, но всё ещё допускает быстрый разгон без drag

## Связанные файлы (для исправления)

- `code_app/drone_sim/models/dynamics.py` — функция `quad_dynamics_16` (вариант 1)
- `code_app/drone_sim/simulation/runner.py` — после `rk4_step` (вариант 2)
- `code_app/drone_sim/simulation/path_sim.py` — `SimConfig.L`, `PathFollowingController.step` (варианты 3, 4)
- `code_app/drone_sim/models/quad_model.py` — добавить параметр `drag` (вариант 1)
