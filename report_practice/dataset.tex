%! Author = Admin
%! Date = 17.04.2026
\section{Сбор датасета ограниченного семейства кривых}
\label{sec:dataset}

Нейросетевая часть проекта не обучается напрямую на траекториях управления. 
Вместо этого формируется датасет состояний и целевых значений параметрической скорости $V^{\ast}$, которая максимальна, но при этом не нарушает условия устойчивого слежения. 
Генерация датасета реализована путем перебора различных случайных параметров типовых кривых и перебором отпимальной скорости на каждом участке заданной кривой.

\subsection{Семейство допустимых кривых}

В датасет включаются только кривые, совместимые с базовым регулятором, то есть удовлетворяющие условию
\begin{gather}
    \|t(s)\| = \mathrm{const}.
\end{gather}
Используются три типа траекторий:
\begin{enumerate}
    \item прямые;
    \item окружности;
    \item спирали.
\end{enumerate}

Для генератора кривых задаются вероятности выбора типов $(0.1,\ 0.4,\ 0.5)$ соответственно. Такое распределение увеличивает долю траекторий с ненулевой кривизной, поскольку именно они ограничивают допустимую скорость движения и дают информативные примеры для обучения.

\subsection{Формирование обучающего примера}

Для каждой кривой выбираются точки $s \in [s_{\min}, s_{\max}]$, после чего в их окрестности формируется начальное состояние
\begin{gather}
    x_0 = x_{\mathrm{curve}}(s) + \Delta x,
\end{gather}
где $\Delta x$ - малое случайное возмущение положения и скорости.
Это необходимо, чтобы признаки в датасете не вырождались в нули и отражали реальные отклонения от траектории.\\
Для каждого состояния извлекается вектор признаков:
\begin{gather}
    f =
    \begin{bmatrix}
        e_1 &
        e_2 &
        \dot{e}_2 &
        \|v\| &
        e_{\mathrm{head}} &
        \kappa_c &
        \kappa_{\max}
    \end{bmatrix}^{\top},
\end{gather}
где:
\begin{gather}
    e_{\mathrm{head}} = \arccos\left(
        \frac{v^{\top} t(s)}{\|v\| \, \|t(s)\|}
    \right),
\end{gather}
\begin{gather}
    \kappa_c(s) =
    \frac{\|t(s) \times t'(s)\|}{\|t(s)\|^3},
\end{gather}
\begin{gather}
    \kappa_{\max}(s) = \max_{\tau \in [s,\ s+\Delta s]} \kappa_c(\tau).
\end{gather}

Большинство признаков нормируются по ограничениям объекта:
\begin{gather}
    \tilde{e}_1 = \frac{e_1}{e_{1,\max}}, \qquad
    \tilde{e}_2 = \frac{e_2}{e_{2,\max}}, \qquad
    \tilde{\dot{e}}_2 = \frac{\dot{e}_2}{v_{\max}}, \qquad
    \widetilde{\|v\|} = \frac{\|v\|}{V_{\max}}.
\end{gather}

Физический смысл каждого признака поясняется в таблице~\ref{tab:features}.

\begin{longtable}{|c|c|>{\raggedright\arraybackslash}p{8.6cm}|}
    \caption{Вектор признаков нейросетевого оптимизатора~$V^{\ast}$}
    \label{tab:features} \\
    \hline
    \textnumero & Признак & Физический смысл \\
    \hline
    \endfirsthead

    \hline
    \textnumero & Признак & Физический смысл \\
    \hline
    \endhead

    \hline
    \endfoot

    \hline
    \endlastfoot

        1 & $\tilde{e}_1$ &
            Нормированная тангенциальная ошибка слежения по касательной Френе.
            Большое $|e_1|$ означает, что дрон опережает или отстаёт от опорной точки;
            при высокой $V^{\ast}$ ошибка растёт. \\\hline
        2 & $\tilde{e}_2$ &
            Нормированная поперечная ошибка (боковое отклонение от кривой).
            Основной критерий безопасности: нейросеть должна не допускать $|e_2| > e_{2,\max}$. \\\hline
        3 & $\tilde{\dot{e}}_2$ &
            Нормированная скорость изменения поперечной ошибки
            (проекция вектора скорости на нормаль кривой).
            Характеризует тенденцию: уходит ли дрон от траектории или возвращается. \\\hline
        4 & $\widetilde{\|v\|}$ &
            Нормированная норма вектора линейной скорости дрона.
            Отражает текущий динамический режим и близость к физическим ограничениям. \\\hline
        5 & $e_{\mathrm{head}}$ &
            Угол между вектором скорости и касательной к кривой.
            При правильном слежении $e_{\mathrm{head}} \approx 0$; большой угол сигнализирует
            о рассогласовании курса и направления движения. \\\hline
        6 & $\kappa_c$ &
            Кривизна кривой в текущей опорной точке.
            На участках с большой кривизной требуется меньшая $V^{\ast}$
            для удержания допустимой боковой ошибки. \\\hline
        7 & $\kappa_{\max}$ &
            Максимальная кривизна на lookahead-окне $[s,\, s + \Delta s]$ впереди по траектории.
            Позволяет нейросети заблаговременно снижать скорость перед сложными участками. \\
\end{longtable}

\subsection{Оракул выбора целевой скорости}

Целевая переменная $V_{\mathrm{opt}}$ для каждой записи ищется с помощью оракула. Оракул перебирает значения скорости в диапазоне
\begin{gather}
    V \in [V_{\min}, V_{\max}]
\end{gather}
с заданным шагом и выбирает наибольшее значение, при котором короткий rollout остается устойчивым:
\begin{gather}
    V_{\mathrm{opt}} = \max \left\{ V | \mathrm{Stable}(x_0,\mathrm{curve},V) = True \right\}.
\end{gather}

Устойчивость rollout определяется тремя критериями:
\begin{gather}
    \max_t |e_2(t)| \le e_{2,\max},
\end{gather}
\begin{gather}
    \max_t \|v(t)\| \le v_{\max},
\end{gather}
\begin{gather}
    x(t) \ \text{не содержит NaN и Inf}.
\end{gather}

В конфигурации проекта для оракула по умолчанию используются параметры
\begin{gather}
    \Delta t_{\mathrm{oracle}} = 0.01, \qquad \kappa_{\mathrm{oracle}} = 100,
\end{gather}
а горизонт rollout автоматически выбирается из длины участка между соседними sample-точками, чтобы дрон успевал пройти достаточно информативный отрезок траектории.

\subsection{Структура датасета}

Сохраняемая запись CSV содержит поля
\begin{gather}
    \{e_1, e_2, \dot{e}_2, \|v\|, e_{\mathrm{head}}, \kappa_c, \kappa_{\max}, s, \|t\|, V_{\mathrm{opt}}\}.
\end{gather}
Из них первые семь признаков используются как вход нейросети, а $V_{\mathrm{opt}}$ является целевой переменной.
Поля $s$ и $\|t\|$ сохраняются для диагностики и последующего анализа распределения примеров по кривым.

\subsection{Предварительная обработка и разбиение}

Перед обучением выполняются следующие шаги подготовки:
\begin{enumerate}
    \item валидация кривой на соответствие ограничению $\|t(s)\|=\mathrm{const}$;
    \item генерация возмущенных состояний вокруг кривой;
    \item извлечение и нормализация признаков;
    \item поиск $V_{\mathrm{opt}}$ с помощью оракула;
    \item отбрасывание неустойчивых или некорректных rollout;
    \item сохранение итоговой таблицы в \texttt{dataset.csv}.
\end{enumerate}

При обучении нейросети датасет далее делится на обучающую и валидационную части в отношении $0.8/0.2$.
Такое разбиение обеспечивает стабильное обучение и тестирование модели.
